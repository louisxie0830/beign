%Image::ExifTool::UserDefined = (
  'Image::ExifTool::XMP::pdfx' => {
     BeingSigningVersion => {
         Writable => 'string',
     },
     BeingSigningId => {
         Writable => 'string',
     },
  },
);
1; # end
